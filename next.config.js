/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aquí puedes añadir configuración de Next.js en el futuro si la necesitas.
  // Por ahora, lo dejamos vacío.
};

module.exports = nextConfig;